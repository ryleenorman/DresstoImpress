@page
@model DresstoImpress.Pages.USERSCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.USERS[0].UserDoB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.USERS[0].UserOccupation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.USERS) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserDoB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserOccupation)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UserID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
