@page
@model DresstoImpress.Pages.WeatherCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].WeatherType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].Temperature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].Precipitation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Weather) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WeatherType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Temperature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precipitation)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WeatherID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WeatherID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WeatherID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
