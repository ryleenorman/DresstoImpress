@page
@model DresstoImpress.Pages.ClothingBySeasonCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClothingBySeason[0].ClothingSeason)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ClothingBySeason) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClothingSeason)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ClothingID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ClothingID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClothingID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
